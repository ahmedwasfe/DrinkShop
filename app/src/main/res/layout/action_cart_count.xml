<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    style="@android:style/Widget.ActionButton">

    <ImageView
        android:id="@+id/img_cart_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_count_cart"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <com.nex3z.notificationbadge.NotificationBadge
        android:id="@+id/badge_cart_count"
        android:layout_width="28dp"
        android:layout_height="28dp"
        app:nbMaxTextLength="2"
        android:layout_marginStart="-10dp"
        android:layout_marginTop="-10dp"
        android:layout_marginBottom="10dp"
        app:layout_constraintEnd_toEndOf="@id/img_cart_count"
        app:layout_constraintBottom_toBottomOf="@id/img_cart_count"
        app:layout_constraintStart_toEndOf="@id/img_cart_count"/>

</androidx.constraintlayout.widget.ConstraintLayout>